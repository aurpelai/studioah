import RouteDefinitions from './RouteDefinitions';
import { RouteType } from './RouteDefinitions.types';

const NavigationMenuDefinitions: RouteType[] = [
  RouteDefinitions.Projects,
  RouteDefinitions.About,
  RouteDefinitions.Contact,
];

export default NavigationMenuDefinitions;
