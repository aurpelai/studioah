import Arrow from './Arrow';
import Dots from './Dots';
import Filter from './Filter';
import { SvgIconType } from '../Icon.types';

const icons: Record<string, React.FC<SvgIconType>> = {
  Arrow,
  Dots,
  Filter,
};

export default icons;
