export interface SvgIconType {
  fill: string,
  size: string,
  stroke: string,
  strokeWidth?: string,
}

export interface IconType {
  fill?: string,
  iconId: string,
  rotation?: number,
  size?: string,
  stroke?: string,
  strokeWidth?: string,
  title?: string,
}

export interface StyledIconType {
  rotation?: number,
}
